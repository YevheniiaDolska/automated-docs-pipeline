# Generated from planning notes for Auto-Doc Pipeline
schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Query {
  health(id: ID): String!
  project(id: ID): String!
}

type Mutation {
  create_project(id: ID): String!
}

type Subscription {
  project_updated(id: ID): String!
  task_completed(id: ID): String!
}
