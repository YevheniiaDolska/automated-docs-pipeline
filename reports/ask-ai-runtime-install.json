{
  "installed": true,
  "runtime_dir": "ask-ai-runtime",
  "next_steps": [
    "cd ask-ai-runtime",
    "cp .env.example .env",
    "python3 -m venv .venv",
    "source .venv/bin/activate",
    "pip install -r requirements.txt",
    "uvicorn app.main:app --host 0.0.0.0 --port 8090"
  ]
}
