asyncapi: 2.6.0
info:
  title: VeriOps Events
  version: 1.0.0
channels:
  project.updated:
    publish:
      message:
        payload:
          type: object
          properties:
            project_id:
              type: string
            status:
              type: string
