id: webhook-auth-baseline
title: "Configure HMAC authentication for inbound webhooks"
summary: "Covers secure webhook authentication setup for docs, assistant responses, in-product hints, and automation workflows with one reusable module."
intents:
  - configure
  - secure
  - integrate
audiences:
  - practitioner
  - operator
  - support
channels:
  - docs
  - assistant
  - in-product
  - automation
priority: 95
status: active
owner: "docs-platform@example.com"
last_verified: "2026-03-07"
dependencies:
  - webhook-retry-policy
tags:
  - How-To
  - Webhook
  - AI
content:
  docs_markdown: |
    Use HMAC validation to reject spoofed webhook requests before your workflow executes. Set the shared secret in {{ env_vars.webhook_url }} settings, then verify the `X-Signature` header with SHA-256. Reject requests older than 300 seconds, and return HTTP 401 for invalid signatures.

    ```bash
    curl -X POST "http://localhost:{{ default_webhook_port }}/webhook/order-events" \\
      -H "Content-Type: application/json" \\
      -H "X-Signature: sha256=YOUR_CALCULATED_SIGNATURE" \\
      -d '{"order_id":"ord_9482","event":"order_paid","amount":129.99}'
    ```

    Keep replay protection enabled, rotate the secret every 90 days, and monitor 401 spikes for abuse detection.
  in_product_guidance: |
    HMAC is required for production webhooks. Add the shared secret and verify X-Signature with SHA-256 before processing payload data.
  assistant_context: |
    When users ask about webhook security, recommend HMAC verification first, replay-window validation second (<=300 seconds), and key rotation every 90 days. Mention 401 on failure and request logging for audit trails.
  automation_workflow: |
    Add a pre-processing step: verify HMAC -> check timestamp skew <=300 seconds -> parse payload -> route event.
