version: '3.9'
services:
  docs-ops:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: bash -lc "npm install && python3 -m pip install -r requirements.txt && make validate"
