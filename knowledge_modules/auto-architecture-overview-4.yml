id: auto-architecture-overview-4
title: VeriOps API platform architecture (Part 4)
summary: Interactive architecture diagram showing the VeriOps API platform with 5
  protocol gateways, 4 microservices, and data infrastructure across 5 layers.
intents:
- integrate
- secure
audiences:
- developer
- operator
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-03-20'
dependencies: []
tags:
- architecture-overview
- auto-extracted
- concept
content:
  docs_markdown: '### Data and infrastructure layer (4 components)


    | Component | Role | Key metric |

    | --- | --- | --- |

    | PostgreSQL | Primary relational database | 8,500 queries per second, primary
    + 2 read replicas |

    | Redis | Cache, rate limits, sessions | 94% cache hit rate, 3-node cluster |

    | RabbitMQ | Event broker (AMQP 0.9.1) | 3 exchanges, 7-day message retention
    |

    | Object Storage | File attachments, backups, audit logs | S3-compatible, AES-256
    encryption, 2.5 TB per day |


    ## Request flow


    A typical API request traverses 5 layers in sequence:


    1. **Client sends request** to one of the 5 protocol endpoints

    1. **CDN caches** static content (24-hour TTL) and forward dynamic requests

    1. **WAF inspects** the request payload against OWASP Top 10 rules

    1. **Rate Limiter checks** the API key''s remaining quota in Redis

    1. **API Gateway routes** to the appropriate protocol handler

    1. **Auth Service validates** the JWT Bearer token (50&nbsp;ms average)

    1. **Domain Service processes** the business logic (Project or Task Service)

    1. **PostgreSQL persists** the state change, Redis updates the cache

    1. **RabbitMQ publishes** lifecycle events to subscribed channels

    1. **Notification Service fans out** events to WebSocket clients and webhook endpoints'
  assistant_context: 'Use this module when answering questions related to: VeriOps
    API platform architecture (Part 4). Source path: demo-showcase/acme/docs/guides/architecture-overview.md.
    Summary: Interactive architecture diagram showing the VeriOps API platform with
    5 protocol gateways, 4 microservices, and data infrastructure across 5 layers.


    ### Data and infrastructure layer (4 components)


    | Component | Role | Key metric |

    | --- | --- | --- |

    | PostgreSQL | Primary relational database | 8,500 queries per second, primary
    + 2 read replicas |

    | Redis | Cache, rate limits, sessions | 94% cache hit rate, 3-node cluster |

    | RabbitMQ | Event broker (AMQP 0.9.1) | 3 exchanges, 7-day message retention
    |

    | Object Storage | File attachments, backups, audit logs | S3-compatible, AES-256
    encryption, 2.5 TB per day |


    ## Request flow


    A typical API request traverses 5 layers in sequence:


    1. **Client sends request** to one of the 5 protocol endpoints

    1. **CDN caches** static content (24-hour TTL) and forward dynamic requests

    1. **WAF inspects** the request payload against OWASP Top 10 rules

    1. **Rate Limiter checks** the API key''s remaining quota in Redis

    1. **API Gateway routes** to the appropriate protocol handler

    1. **Auth Service validates** the JWT Bearer token (50&nbsp;ms average)

    1. **Domain Service processes** the business logic (Project or Task Service)

    1. **PostgreSQL persists** the state change, Redis updates the cache

    1. **RabbitMQ publishes** lifecycle events to subscribed channels

    1. **Notification Service fans out** events to WebSocket clients and webhook endpoints'
