id: auto-how-to-5
title: 'How-to: keep docs aligned with every release (Part 5)'
summary: Run the unified autopipeline on release day to synchronize API documentation
  across five protocols with quality gates and review manifests.
intents:
- configure
audiences:
- practitioner
- developer
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-03-20'
dependencies: []
tags:
- auto-extracted
- how-to
content:
  docs_markdown: '## Step 3: review the manifest


    Open the review manifest to see all generated artifacts and their availability:


    ```bash

    cat reports/acme-demo/REVIEW_MANIFEST.md

    ```


    The manifest lists every artifact, stage status, and provides a reviewer checklist.
    Key sections:


    | Section | What to check |

    | --- | --- |

    | Pipeline execution summary | Exit code, strictness mode, artifact counts |

    | Stage availability | Which stages produced artifacts, which are missing |

    | Reviewer checklist | Approval gates before publish |


    You can also check the machine-readable manifest:


    ```bash

    python3 -c "

    import json

    m = json.load(open(''reports/acme-demo/review_manifest.json''))

    print(f''Available: {m[\"available_artifacts\"]}'')

    print(f''Missing: {m[\"missing_artifacts\"]}'')

    print(f''Strictness: {m[\"strictness\"]}'')"

    ```'
  assistant_context: 'Use this module when answering questions related to: How-to:
    keep docs aligned with every release (Part 5). Source path: demo-showcase/acme/docs/guides/how-to.md.
    Summary: Run the unified autopipeline on release day to synchronize API documentation
    across five protocols with quality gates and review manifests.


    ## Step 3: review the manifest


    Open the review manifest to see all generated artifacts and their availability:


    ```bash

    cat reports/acme-demo/REVIEW_MANIFEST.md

    ```


    The manifest lists every artifact, stage status, and provides a reviewer checklist.
    Key sections:


    | Section | What to check |

    | --- | --- |

    | Pipeline execution summary | Exit code, strictness mode, artifact counts |

    | Stage availability | Which stages produced artifacts, which are missing |

    | Reviewer checklist | Approval gates before publish |


    You can also check the machine-readable manifest:


    ```bash

    python3 -c "

    import json

    m = json.load(open(''reports/acme-demo/review_manifest.json''))

    print(f''Available: {m[\"available_artifacts\"]}'')

    print(f''Missing: {m[\"missing_artifacts\"]}'')

    print(f''Strictness: {m[\"strictness\"]}'')"

    ```'
