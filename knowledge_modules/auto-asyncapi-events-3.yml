id: auto-asyncapi-events-3
title: AsyncAPI event docs (Part 3)
summary: AsyncAPI 2.6.0 event documentation for VeriOps with channel contracts, delivery
  semantics, payload schemas, and interactive event tester.
intents:
- integrate
audiences:
- developer
- operator
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-03-20'
dependencies: []
tags:
- asyncapi-events
- auto-extracted
- reference
content:
  docs_markdown: '### Envelope fields


    | Field | Type | Description |

    | --- | --- | --- |

    | `event_id` | string | Unique event identifier for idempotency (format: `evt_*`)
    |

    | `event_type` | string | Channel name matching the AsyncAPI contract |

    | `occurred_at` | string (ISO 8601) | Timestamp when the event occurred |

    | `producer` | string | Service that emitted the event |

    | `trace_id` | string | Distributed tracing ID for correlation |

    | `schema_version` | string | Payload schema version (semver) |

    | `data` | object | Domain-specific payload (see below) |'
  assistant_context: 'Use this module when answering questions related to: AsyncAPI
    event docs (Part 3). Source path: demo-showcase/acme/docs/reference/asyncapi-events.md.
    Summary: AsyncAPI 2.6.0 event documentation for VeriOps with channel contracts,
    delivery semantics, payload schemas, and interactive event tester.


    ### Envelope fields


    | Field | Type | Description |

    | --- | --- | --- |

    | `event_id` | string | Unique event identifier for idempotency (format: `evt_*`)
    |

    | `event_type` | string | Channel name matching the AsyncAPI contract |

    | `occurred_at` | string (ISO 8601) | Timestamp when the event occurred |

    | `producer` | string | Service that emitted the event |

    | `trace_id` | string | Distributed tracing ID for correlation |

    | `schema_version` | string | Payload schema version (semver) |

    | `data` | object | Domain-specific payload (see below) |'
