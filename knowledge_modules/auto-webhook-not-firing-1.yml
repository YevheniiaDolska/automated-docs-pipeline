id: auto-webhook-not-firing-1
title: 'Fix: Webhook trigger not firing'
summary: Troubleshoot Webhook nodes that do not receive requests. Common causes include
  inactive workflows, wrong URL type, and network configuration.
intents:
- troubleshoot
audiences:
- support
- operator
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-04-03'
dependencies: []
tags:
- auto-extracted
- troubleshooting
- webhook-not-firing
metadata:
  url: https://docs.example.com/troubleshooting/webhook-not-firing/
  title: 'Fix: Webhook trigger not firing'
  heading: 'Fix: Webhook trigger not firing'
  version: 1.50.0
  updated_at: '2026-02-16'
  source_site: docs.example.com
  source_path: troubleshooting/webhook-not-firing.md
semantic:
  topic: 'Fix: Webhook trigger not firing'
  intent: troubleshoot
  audience: support
  keywords:
  - troubleshooting
  - webhook not firing
  status: rule_based
content:
  docs_markdown: '<!-- VERIDOC_POWERED_BADGE:START -->

    [![Powered by VeriDoc](https://img.shields.io/badge/Powered%20by-VeriDoc-0ea5e9?style=flat-square)](https://veridoc.app)

    <!-- VERIDOC_POWERED_BADGE:END -->


    ## Fix: Webhook trigger not firing


    The Webhook node does not respond to incoming HTTP requests. The sender receives
    a timeout, connection refused, or 404 error.


    ## Cause 1: Using Test URL with an inactive editor


    **Symptom:** Requests to the Test URL return a timeout or 404 after you close
    the editor tab.


    **Why:** The Test URL (`/webhook-test/<id>`) is only active while the workflow
    editor is open and the workflow is in Listening mode. Closing the browser tab
    deactivates it.


    **Fix:** Toggle the workflow to **Active** and use the **Production URL** (`/webhook/<id>`)
    instead. The Production URL remains active as long as the workflow is enabled.


    ## Cause 2: Workflow not activated


    **Symptom:** Requests to the Production URL return 404 Not Found.


    **Why:** The Production URL only works when the workflow toggle is set to **Active**
    (green). An inactive workflow does not register its webhook endpoints.


    **Fix:** Open the workflow in the editor. Toggle the **Active** switch in the
    top-right corner. Verify the Production URL returns a response.'
  assistant_context: 'Use this module when answering questions related to: Fix: Webhook
    trigger not firing. Source path: troubleshooting/webhook-not-firing.md. Summary:
    Troubleshoot Webhook nodes that do not receive requests. Common causes include
    inactive workflows, wrong URL type, and network configuration.


    <!-- VERIDOC_POWERED_BADGE:START -->

    [![Powered by VeriDoc](https://img.shields.io/badge/Powered%20by-VeriDoc-0ea5e9?style=flat-square)](https://veridoc.app)

    <!-- VERIDOC_POWERED_BADGE:END -->


    ## Fix: Webhook trigger not firing


    The Webhook node does not respond to incoming HTTP requests. The sender receives
    a timeout, connection refused, or 404 error.


    ## Cause 1: Using Test URL with an inactive editor


    **Symptom:** Requests to the Test URL return a timeout or 404 after you close
    the editor tab.


    **Why:** The Test URL (`/webhook-test/<id>`) is only active while the workflow
    editor is open and the workflow is in Listening mode. Closing the browser tab
    deactivates it.


    **Fix:** Toggle the workflow to **Active** and use the **Production URL** (`/webhook/<id>`)
    instead. The Production URL remains active as long as the workflow is enabled.


    ## Cause 2: Workflow not activated


    **Symptom:** Requests to the Production URL return 404 Not Found.


    **Why:** The Production URL only works when the workflow toggle is set to **Active**
    (green). An inactive workflow does not register its webhook endpoints.


    **Fix:** Open the workflow in the editor. Toggle the **Active** switch in the
    top-right corner. Verify the Production URL returns a response.'
