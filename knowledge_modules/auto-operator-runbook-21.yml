id: auto-operator-runbook-21
title: Operator Runbook (Retainer Operations) (Part 21)
summary: Step-by-step instructions for weekly report review, client questions, new
  repo setup, and profile tuning.
intents:
- configure
- troubleshoot
audiences:
- practitioner
- developer
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-03-28'
dependencies: []
tags:
- auto-extracted
- how-to
- operator-runbook
content:
  docs_markdown: '### Scheduler did not run


    1. Check if the cron job (Linux) or Windows Task (Windows) exists:


    Linux:


    ```bash

    crontab -l | grep docsops

    ```


    Windows:


    ```bash

    schtasks /query /tn "VeriOps Weekly"

    ```


    1. If missing, re-install:


    Linux:


    ```bash

    bash <client-repo>/docsops/ops/install_cron_weekly.sh

    ```


    Windows:


    ```bash

    powershell -ExecutionPolicy Bypass -File <client-repo>/docsops/ops/install_windows_task.ps1

    ```


    1. Check git access: the scheduler runs under the user account that installed
    it. That account must have `git pull` access to the repo (SSH key or credential
    helper).


    1. Check logs: `<client-repo>/reports/docsops-weekly.log`.


    ### Pipeline fails with license error


    ```text

    [license] BLOCKED: Feature ''drift_detection'' requires a plan upgrade (current:
    community).

    ```


    Cause: License file is missing, expired, or corrupted.


    Fix:


    1. Check `docsops/license.jwt` exists and is not empty.

    1. Check expiration: `python3 docsops/scripts/license_gate.py` (prints license
    summary).

    1. If expired, generate a new license and send it to the client.

    1. For dev/test: `export VERIOPS_LICENSE_PLAN=enterprise`.'
  assistant_context: 'Use this module when answering questions related to: Operator
    Runbook (Retainer Operations) (Part 21). Source path: docs/operations/OPERATOR_RUNBOOK.md.
    Summary: Step-by-step instructions for weekly report review, client questions,
    new repo setup, and profile tuning.


    ### Scheduler did not run


    1. Check if the cron job (Linux) or Windows Task (Windows) exists:


    Linux:


    ```bash

    crontab -l | grep docsops

    ```


    Windows:


    ```bash

    schtasks /query /tn "VeriOps Weekly"

    ```


    1. If missing, re-install:


    Linux:


    ```bash

    bash <client-repo>/docsops/ops/install_cron_weekly.sh

    ```


    Windows:


    ```bash

    powershell -ExecutionPolicy Bypass -File <client-repo>/docsops/ops/install_windows_task.ps1

    ```


    1. Check git access: the scheduler runs under the user account that installed
    it. That account must have `git pull` access to the repo (SSH key or credential
    helper).


    1. Check logs: `<client-repo>/reports/docsops-weekly.log`.


    ### Pipeline fails with license error


    ```text

    [license] BLOCKED: Feature ''drift_detection'' requires a plan upgrade (current:
    community).

    ```


    Cause: License file is missing, expired, or corrupted.


    Fix:


    1. Check `docsops/license.jwt` exists and is not empty.

    1. Check expiration: `python3 docsops/scripts/license_gate.py` (prints license
    summary).

    1. If expired, generate a new license and send it to the client.

    1. For dev/test: `export VERIOPS_LICENSE_PLAN=enterprise`.'
