id: auto-pipeline-capabilities-catalog-16
title: Pipeline Capabilities Catalog (Part 16)
summary: Generated catalog of available pipeline commands, templates, policy packs,
  and assets for client configuration.
intents:
- integrate
audiences:
- developer
- operator
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-04-18'
dependencies: []
tags:
- auto-extracted
- pipeline-capabilities-catalog
- reference
metadata:
  url: https://docs.example.com/operations/PIPELINE_CAPABILITIES_CATALOG/
  title: Pipeline Capabilities Catalog (Part 16)
  heading: RAG contradiction and stale guardrails
  version: 1.50.0
  updated_at: '2026-04-17T00:00:00Z'
  source_site: docs.example.com
  source_path: operations/PIPELINE_CAPABILITIES_CATALOG.md
semantic:
  topic: RAG contradiction and stale guardrails
  intent: integrate
  audience: developer
  keywords:
  - pipeline capabilities catalog
  - reference
  status: rule_based
content:
  docs_markdown: '1. `extract_knowledge_modules_from_docs.py` -- auto-chunk docs into
    knowledge modules

    1. `validate_knowledge_modules.py` -- schema validation, duplicate ID detection,
    cycle check

    1. `detect_rag_contradictions.py` -- detects cross-module contradictions in critical
    runtime facts and writes `reports/rag_contradictions_report.json`

    1. `generate_knowledge_retrieval_index.py` -- JSON index for Algolia + FAISS input,
    excluding critical conflicting modules by default

    1. `generate_embeddings.py` -- FAISS vector index (`text-embedding-3-small`, 1536
    dims)

    1. `generate_knowledge_graph_jsonld.py` -- JSON-LD knowledge graph

    1. `run_retrieval_evals.py` -- precision@k, recall@k, hallucination rate evaluation


    ## RAG contradiction and stale guardrails


    Two complementary guardrails now run for retrieval quality:


    1. Stale-check answers "is this document outdated?"

    1. Contradiction-check answers "do active documents conflict right now?"


    When contradictions are detected:


    1. Critical conflicts are excluded from retrieval index generation.

    1. Warnings remain visible for review but do not automatically block indexing.

    1. Optional gate mode can fail the run on critical contradictions.


    This design prevents low-trust answers where RAG cites conflicting facts from
    two active modules.'
  assistant_context: 'Use this module when answering questions related to: Pipeline
    Capabilities Catalog (Part 16). Source path: operations/PIPELINE_CAPABILITIES_CATALOG.md.
    Summary: Generated catalog of available pipeline commands, templates, policy packs,
    and assets for client configuration.


    1. `extract_knowledge_modules_from_docs.py` -- auto-chunk docs into knowledge
    modules

    1. `validate_knowledge_modules.py` -- schema validation, duplicate ID detection,
    cycle check

    1. `detect_rag_contradictions.py` -- detects cross-module contradictions in critical
    runtime facts and writes `reports/rag_contradictions_report.json`

    1. `generate_knowledge_retrieval_index.py` -- JSON index for Algolia + FAISS input,
    excluding critical conflicting modules by default

    1. `generate_embeddings.py` -- FAISS vector index (`text-embedding-3-small`, 1536
    dims)

    1. `generate_knowledge_graph_jsonld.py` -- JSON-LD knowledge graph

    1. `run_retrieval_evals.py` -- precision@k, recall@k, hallucination rate evaluation


    ## RAG contradiction and stale guardrails


    Two complementary guardrails now run for retrieval quality:


    1. Stale-check answers "is this document outdated?"

    1. Contradiction-check answers "do active documents conflict right now?"


    When contradictions are detected:


    1. Critical conflicts are excluded from retrieval index generation.

    1. Warnings remain visible for review but do not automatically block indexing.

    1. Optional gate mode can fail the run on critical contradictions.


    This design prevents low-trust answers where RAG cites conflicting facts from
    two active modules.'
