id: auto-pipeline-capabilities-catalog-1
title: Pipeline Capabilities Catalog
summary: Generated catalog of available pipeline commands, templates, policy packs,
  and assets for client configuration.
intents:
- integrate
audiences:
- developer
- operator
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-03-19'
dependencies: []
tags:
- auto-extracted
- pipeline-capabilities-catalog
- reference
content:
  docs_markdown: '# Pipeline Capabilities Catalog


    This catalog has two layers:


    1. npm script entry points (from `package.json`)

    1. direct CLI entry points in `scripts/*.py` that are used by provisioning/operator
    flows and are not exposed as npm commands


    Non-script concepts (policy semantics, sales packaging, pilot/full scope) are
    documented in ops guides.


    Use this catalog with `runtime.custom_tasks.weekly` in client profiles to enable
    any capability.'
  assistant_context: 'Use this module when answering questions related to: Pipeline
    Capabilities Catalog. Source path: docs/operations/PIPELINE_CAPABILITIES_CATALOG.md.
    Summary: Generated catalog of available pipeline commands, templates, policy packs,
    and assets for client configuration.


    # Pipeline Capabilities Catalog


    This catalog has two layers:


    1. npm script entry points (from `package.json`)

    1. direct CLI entry points in `scripts/*.py` that are used by provisioning/operator
    flows and are not exposed as npm commands


    Non-script concepts (policy semantics, sales packaging, pilot/full scope) are
    documented in ops guides.


    Use this catalog with `runtime.custom_tasks.weekly` in client profiles to enable
    any capability.'
