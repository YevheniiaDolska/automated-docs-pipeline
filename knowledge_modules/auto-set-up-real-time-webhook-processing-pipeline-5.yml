id: auto-set-up-real-time-webhook-processing-pipeline-5
title: Set up a real-time webhook processing pipeline (Part 5)
summary: Configure end-to-end webhook ingestion with HMAC verification, async queue
  processing, and delivery guarantees in under 15 minutes.
intents:
- configure
audiences:
- practitioner
- developer
channels:
- docs
- assistant
- automation
priority: 60
status: active
owner: docsops-auto
last_verified: '2026-04-15'
dependencies: []
tags:
- auto-extracted
- how-to
- set-up-real-time-webhook-processing-pipeline
metadata:
  url: https://docs.example.com/en/how-to/set-up-real-time-webhook-processing-pipeline/
  title: Set up a real-time webhook processing pipeline (Part 5)
  heading: JavaScript HMAC verification
  version: '18.0'
  updated_at: '2026-03-17T00:00:00Z'
  source_site: docs.example.com
  source_path: en/how-to/set-up-real-time-webhook-processing-pipeline.md
semantic:
  topic: JavaScript HMAC verification
  intent: configure
  audience: practitioner
  keywords:
  - how-to
  - set up real time webhook processing pipeline
  status: rule_based
content:
  docs_markdown: "### JavaScript HMAC verification\n\n```javascript\nconst crypto\
    \ = require('crypto');\n\nfunction verifyWebhookSignature(payload, signatureHeader,\
    \ secret) {\n  // Parse signature header: \"t=<timestamp>,v1=<signature>\"\n \
    \ const parts = {};\n  signatureHeader.split(',').forEach(part => {\n    const\
    \ [key, ...rest] = part.split('=');\n    parts[key.trim()] = rest.join('=').trim();\n\
    \  });\n\nconst timestamp = parts.t;\n  const receivedSig = parts.v1;\n\nif (!timestamp\
    \ || !receivedSig) {\n    return false;\n  }\n\n// Reject events older than 5\
    \ minutes (replay protection)\n  const currentTime = Math.floor(Date.now() / 1000);\n\
    \  if (Math.abs(currentTime - parseInt(timestamp, 10)) > 300) {\n    return false;\n\
    \  }\n\n// Compute expected signature: HMAC-SHA256(secret, timestamp.payload)\n\
    \  const signedContent = `${timestamp}.${payload}`;\n  const expectedSig = crypto\n\
    \    .createHmac('sha256', secret)\n    .update(signedContent)\n    .digest('hex');\n\
    \n// Timing-safe comparison to prevent timing attacks\n  return crypto.timingSafeEqual(\n\
    \    Buffer.from(expectedSig),\n    Buffer.from(receivedSig)\n  );\n}"
  assistant_context: "Use this module when answering questions related to: Set up\
    \ a real-time webhook processing pipeline (Part 5). Source path: en/how-to/set-up-real-time-webhook-processing-pipeline.md.\
    \ Summary: Configure end-to-end webhook ingestion with HMAC verification, async\
    \ queue processing, and delivery guarantees in under 15 minutes.\n\n### JavaScript\
    \ HMAC verification\n\n```javascript\nconst crypto = require('crypto');\n\nfunction\
    \ verifyWebhookSignature(payload, signatureHeader, secret) {\n  // Parse signature\
    \ header: \"t=<timestamp>,v1=<signature>\"\n  const parts = {};\n  signatureHeader.split(',').forEach(part\
    \ => {\n    const [key, ...rest] = part.split('=');\n    parts[key.trim()] = rest.join('=').trim();\n\
    \  });\n\nconst timestamp = parts.t;\n  const receivedSig = parts.v1;\n\nif (!timestamp\
    \ || !receivedSig) {\n    return false;\n  }\n\n// Reject events older than 5\
    \ minutes (replay protection)\n  const currentTime = Math.floor(Date.now() / 1000);\n\
    \  if (Math.abs(currentTime - parseInt(timestamp, 10)) > 300) {\n    return false;\n\
    \  }\n\n// Compute expected signature: HMAC-SHA256(secret, timestamp.payload)\n\
    \  const signedContent = `${timestamp}.${payload}`;\n  const expectedSig = crypto\n\
    \    .createHmac('sha256', secret)\n    .update(signedContent)\n    .digest('hex');\n\
    \n// Timing-safe comparison to prevent timing attacks\n  return crypto.timingSafeEqual(\n\
    \    Buffer.from(expectedSig),\n    Buffer.from(receivedSig)\n  );\n}"
