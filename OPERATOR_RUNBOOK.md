# Operator runbook (mirror pointer)

Canonical runbook moved to:

- `docs/operations/OPERATOR_RUNBOOK.md`

Reason:

- Avoid duplicate operational guidance in root and docs/operations.
- Keep one source of truth for weekly operations and incident handling.

If you need to edit operator procedures, edit only:

- `docs/operations/OPERATOR_RUNBOOK.md`
